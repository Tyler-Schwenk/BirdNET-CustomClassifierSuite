# Frozen requirements from working Python 3.10.0 environment
# Generated: 2025-12-13
# Use: pip install -r requirements.txt

# Core ML/Audio Stack
tensorflow==2.15.1
numpy==1.26.4
pandas==2.2.3
scikit-learn==1.7.2
librosa==0.11.0
soundfile==0.13.1
matplotlib==3.10.7

# Keras bundled with TensorFlow 2.15 - do NOT install separately
# keras is NOT needed as a separate package

# Hyperparameter tuning
keras-tuner

# Streamlit UI
streamlit
streamlit-aggrid
altair>=5
vl-convert-python>=1.0

# Utilities
pyyaml
tqdm
tabulate==0.9.0

# Testing
pytest>=7.0
pytest-cov>=4.0

# GUI Dependencies
# NOTE: tkinter is part of Python's standard library and cannot be installed via pip
# If tkinter is not available:
#   - Windows: Reinstall Python from python.org ensuring "tcl/tk and IDLE" is checked
#   - Linux: sudo apt-get install python3-tk (Ubuntu/Debian) or yum install python3-tkinter (RHEL/CentOS)
#   - macOS: Typically included; if missing, reinstall Python from python.org

# NOTE: BirdNET-Analyzer is NOT pip installed
# The external/BirdNET-Analyzer directory is used directly
# Do NOT uncomment the following line:
# -e external/BirdNET-Analyzer[train]
